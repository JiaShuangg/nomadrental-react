import React from 'react';
import ReactDOM from 'react-dom/client';
import ChangeWord from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
<React.StrictMode>
  <ChangeWord/>
</React.StrictMode>
);
